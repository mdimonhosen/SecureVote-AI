import 'dart:ui';

class Face {
  final Rect? boundingBox;
  Face({this.boundingBox});
}

class InputImage {
  static dynamic fromFilePath(String path) => null;
  static dynamic fromBytes({required dynamic bytes, required dynamic metadata}) => null;
}
