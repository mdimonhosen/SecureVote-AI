name: vote19
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  
  # Backend
  supabase_flutter: ^2.5.0
  
  # State Management & Routing
  provider: ^6.1.2
  go_router: ^14.0.0
  
  # UI & Styling
  google_fonts: ^6.2.1
  
  # Face Detection
  camera: ^0.11.0
  google_mlkit_face_detection: ^0.11.0
  
  # Date Formatting (ADD THIS LINE)
  intl: ^0.19.0
  crypto: ^3.0.7
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true